# 固定品牌资产

本目录中的 `knowledge_slow_stew_logo.png` 是片尾 C 页必须使用的“知识慢炖”固定 logo。

约束：

1. C 页后处理必须读取该文件，不允许用代码临时绘制 logo。
2. 不允许由 AI 生图模型生成 logo、文字、水印或替代图形。
3. 如果该文件缺失，后处理应直接报错，避免静默回退成错误 logo。
4. A 页不放 logo，只保留书名、作者、章节序号、章节名称、集数、本集小标题。
