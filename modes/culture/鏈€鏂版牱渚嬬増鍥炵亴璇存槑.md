# 最新样例版回灌说明

本版已把最新确认方向固化到 A / C 后处理模板中，重点包括：

1. A 系列与 C 系列统一采用暖纸书房风格。
2. 固定使用原始 `knowledge_slow_stew_logo.png`，并在后处理中以圆形背景承载。
3. 文字框采用强居中布局：
   - `下集预告`
   - `第03集 · 第01章 · 重新理解贫穷` 这类标签
   都在各自承载框内水平、垂直居中。
4. A02 横屏采用左文右图，右侧视觉区铺满，不再留出空白死区。
5. 标题、标签、行距、品牌区位置采用固定模板，减少漂移。
6. A 页底部品牌区、C 页底部品牌区统一采用圆形 logo + 品牌名 + slogan。

主要代码改动：
- `automedia_core/postprocess_playwright.py`

说明：
- 这一版重点是回灌最终样例的版式结构与品牌呈现。
- 如果后续还要继续微调像素级位置，只需继续调整该文件中的 CSS 即可。
